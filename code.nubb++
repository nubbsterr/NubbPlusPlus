# Nubb++ 1.2 Showcase

PRINT "Hello world," 
PRINT "Welcome to Nubb++ 1.2 ❤️"

# Boolean values are now supported!

IF True == True THEN 
PRINT "Boolean values are now supported!"
ENDIF

IF 0 == False THEN
PRINT "We can even have jank expressions like this!"
ENDIF

IF 0 == None THEN
PRINT "We also included the None/NULL keyword!"
ENDIF
