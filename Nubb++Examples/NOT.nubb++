( Compatible ONLY with Nubb++ 2.1 )

PRINT "Enter a number (positive or negative): "

INPUT int n

IF NOT n+1 THEN
    PRINT "NOT operators work now."
ENDIF
PRINT "Entered something > 0, NOT statement didn't trigger!"
